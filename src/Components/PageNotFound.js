import React from 'react';
import{Link} from "react-router-dom"
function PageNotFound(props) {
    return (
        <div>
            PageNotFound <Link to="/">Home</Link>
            
        </div>
    );
}

export default PageNotFound;